# Файловый менеджер на Spring Boot
Реализация файлового менеджера на Spring Boot.

Стек:
* Java 21
* Maven
* Паттерн MVC
* Spring Boot

Во время выполнения проекта соблюдал Java code conversions,
придерживался структуре MVC, использовал Javadoc максимально оптимально,
использовал возможности Spring Boot для реализации проекта.

Создал отдельные аннотации для контроллера, сервиса и представления,
и использовал их.

Использовал профилирование в приложении. Выделил два профиля: dev и logging.
Первый используется по умолчанию и подразумевает обычный процесс выполнения
программы. Второй в свою очереди выводит на экран логи.
Логирование настраивается в файле logback.xml.

Настроил файл конфигурации. Перенес в него типы файлов,
настроил благодаря ему профилирование.

Добавил параметр уровня файлового дерева, благодаря которому
на экране будет изображена древовидная структура определенного уровня.

Поставил для себя задачу выдавать максимальную информацию
об объектах файловой системы, поэтому решил использовать многопоточность
и рекурсию для прохода по файловому дереву. Благодаря этому получилось
выдавать следующую информацию об объектах:
* Объект является директорией или файлом
  (если файл, то дополнительно указывается его тип)
* Размер объекта (для директории 0)
* Права доступа
* Количество содержимых объектов (для файла 0)
* Размер содержимого объекта (для файла 0)

Возможности файлового менеджера:
* Создание нового пустого файла
* Создание новой пустой директории
* Удаление файла или директории
* Переход в директорию или выше по файловому дереву
* Обновление списка объектов